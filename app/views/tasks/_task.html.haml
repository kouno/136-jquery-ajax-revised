= form_for task do |f|
  = f.check_box :complete
  = f.submit "Update"
  = f.label :complete, task.name
  = link_to "(remove)", task, method: :delete, data: {confirm: "Are you sure?"}
  = link_to "(assign to)", edit_task_path(task)
